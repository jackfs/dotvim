Installation: 

  git clone git://github.com/jackfs/dotvim.git ~/.vim

Create synlinks:

  ln -s ~/dotfiles/vimrc   ~/.vimrc        
  ln -s ~/dotfiles/gvimrc  ~/.gvimrc

or 
  mklink ~\.vimrc   ~\dotfiles\vimrc          (where dotfiles is ~\.vim in Windows)
  mklink ~\.gvimrc  ~\dotfiles\gvimrc